# 毕业生实习与就业管理系统 - 部署指南

## 1. 复制项目

将整个项目文件夹拷贝到目标电脑。

## 2. 安装环境

- **JDK 8**
- **Maven 3.x**
- **Node.js**（建议 v14/v16）+ 安装 cnpm：`npm install -g cnpm`
- **MySQL 5.7+**

## 3. 初始化数据库

```bash
mysql -u root -p < db.sql
```

这会创建 `biyeshengshixiyujiuye` 数据库及种子数据（默认密码 `123456`）。

## 4. 修改数据库配置（如有不同）

编辑 `biyeshengshixiyujiuye/src/main/resources/application.yml`，修改 MySQL 连接地址、用户名、密码。

## 5. 启动后端

```bash
cd biyeshengshixiyujiuye
mvn clean package
mvn spring-boot:run
```

后端运行在 `localhost:8080`。

## 6. 启动前端

```bash
cd biyeshengshixiyujiuye/src/main/resources/admin/admin
cnpm install
cnpm run serve
```

前端运行在 `localhost:8081`，自动代理 API 到后端。

## 7. 访问系统

浏览器打开 `http://localhost:8081`

默认管理员账号：`admin` / `admin`